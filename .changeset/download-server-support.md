---
"@neuledge/context": minor
---

Add MCP tools for searching and downloading documentation packages from registry servers. New `search_packages` and `download_package` tools allow AI agents to discover and install pre-built documentation packages. Downloaded packages are automatically available via the `get_docs` tool.
