---
"@neuledge/context": minor
---

Add download server support with `search_packages` and `download_package` MCP tools for discovering and installing pre-built documentation packages from remote servers
